#ifndef __TSARGS_H
#define __TSARGS_H

#include <stdio.h>     /* for printf */
#include <stdlib.h>    /* for exit */
#include <getopt.h>

class TSargs {

public:
	static int init( int argc, char **argv ) ;

};

#endif

